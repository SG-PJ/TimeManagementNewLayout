<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include android:id="@+id/date_home_fragment" layout="@layout/activity_name" />
    <include android:id="@+id/date_home_fragment" layout="@layout/activity_date" />

    <LinearLayout
        android:id="@+id/linearLayout"
        android:layout_width="315dp"
        android:layout_height="18dp"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="@+id/date_home_fragment"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@+id/date_home_fragment">

        <TextView
            android:id="@+id/dispUserNameToday"
            android:layout_width="11dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:background="@drawable/view_frameandbackground"
            android:text="@string/dispResiduePaidHolidays"
            android:textColor="#FFFFFF" />

        <TextView
            android:id="@+id/valueUserNameToday"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/view_frame"
            android:layout_weight="2" />
    </LinearLayout>

    <include
        android:id="@+id/worktime_home_fragment"
        layout="@layout/activity_work_time"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>